# 🔧 Настройка файлов сессий на сервере

## ❌ Проблема

4 broadcaster'а не могут использовать 2 файла сессий одновременно:
- AAA_PRICE + GUS_ADS → acc1.session = **database is locked!**
- GUS_PRICE + AAA_ADS → acc2.session = **database is locked!**

## ✅ Решение

Каждый broadcaster использует УНИКАЛЬНЫЙ файл сессии:
- AAA_PRICE → `acc1_price.session`
- GUS_PRICE → `acc2_price.session`
- AAA_ADS → `acc2_ads.session`
- GUS_ADS → `acc1_ads.session`

**ВАЖНО:** Это просто **копии** одного и того же аккаунта!
- acc1_price и acc1_ads - это ОДИН аккаунт (разные файлы)
- acc2_price и acc2_ads - это ОДИН аккаунт (разные файлы)

---

## 🚀 Настройка на сервере (Windows PowerShell)

### Вариант 1: ПРАВИЛЬНЫЕ аккаунты (рекомендуется)

Сначала исправим перепутанные аккаунты:

```powershell
cd C:\Users\Administrator\PycharmProjects\SendMessageBot_v2\sessions

# Переименовать (сейчас перепутаны)
ren acc1.session acc1_old.session
ren acc2.session acc2_old.session

# Правильно назначить
ren acc1_old.session acc2.session  # Анна → acc2
ren acc2_old.session acc1.session  # Яблочный Гусь → acc1
```

Теперь правильно:
- `acc1.session` = Яблочный Гусь ✅
- `acc2.session` = Анна Макарова ✅

### Создать уникальные копии:

```powershell
# Копии acc1 (Яблочный Гусь)
copy acc1.session acc1_price.session
copy acc1.session acc1_ads.session

# Копии acc2 (Анна Макарова)
copy acc2.session acc2_price.session
copy acc2.session acc2_ads.session

# Проверить
dir *.session
```

Должно быть **6 файлов:**
```
acc1.session         (оригинал - Яблочный Гусь)
acc1_price.session   (копия для AAA_PRICE)
acc1_ads.session     (копия для GUS_ADS)
acc2.session         (оригинал - Анна Макарова)
acc2_price.session   (копия для GUS_PRICE)
acc2_ads.session     (копия для AAA_ADS)
```

---

### Вариант 2: Оставить как есть (работает, но не оптимально)

Если не хотите переименовывать:

```powershell
# Просто создать копии
copy acc1.session acc1_price.session
copy acc1.session acc1_ads.session
copy acc2.session acc2_price.session
copy acc2.session acc2_ads.session
```

Будет работать, но:
- acc1 = Анна (неправильно по логике, но работает)
- acc2 = Яблочный Гусь (неправильно по логике, но работает)

---

## 📊 Итоговое распределение

### После правильной настройки:

| Broadcaster | Сессия | Аккаунт | Назначение |
|-------------|--------|---------|------------|
| AAA_PRICE | acc1_price | Яблочный Гусь | Прайсы AAA |
| GUS_PRICE | acc2_price | Анна Макарова | Прайсы GUS |
| AAA_ADS | acc2_ads | Анна Макарова | Реклама AAA |
| GUS_ADS | acc1_ads | Яблочный Гусь | Реклама GUS |

**Логика:**
- **Прайсы:** AAA→Яблочный Гусь, GUS→Анна
- **Реклама:** AAA→Анна, GUS→Яблочный Гусь (противоположно)

---

## ❓ Почему нужны копии файлов?

### Техническое объяснение:

Telethon использует SQLite для хранения сессий. SQLite:
- ❌ НЕ поддерживает параллельный доступ из разных процессов
- ❌ НЕ поддерживает одновременную запись

**Когда 2 broadcaster'а используют 1 файл:**
```
Broadcaster 1: Открывает acc1.session → LOCK
Broadcaster 2: Пытается открыть acc1.session → ❌ database is locked!
```

**С копиями файлов:**
```
Broadcaster 1: Открывает acc1_price.session → OK
Broadcaster 2: Открывает acc1_ads.session → OK (другой файл!)
```

### Это безопасно?

✅ **ДА! Это полностью безопасно:**
- Это просто копии одной и той же авторизации
- Оба файла содержат данные одного аккаунта
- Telegram видит это как один аккаунт с разных клиентов
- Это стандартная практика для множественных соединений

---

## 🎯 Пошаговая инструкция для сервера

```powershell
# 1. Перейти в папку sessions
cd C:\Users\Administrator\PycharmProjects\SendMessageBot_v2\sessions

# 2. ИСПРАВИТЬ перепутанные аккаунты (ВАЖНО!)
# Сейчас: acc1=Анна, acc2=Яблочный Гусь (НЕПРАВИЛЬНО!)
# Нужно: acc1=Яблочный Гусь, acc2=Анна

ren acc1.session temp.session
ren acc2.session acc1.session
ren temp.session acc2.session

# 3. Создать копии
copy acc1.session acc1_price.session
copy acc1.session acc1_ads.session
copy acc2.session acc2_price.session
copy acc2.session acc2_ads.session

# 4. Проверить
dir *.session

# 5. Запустить бот
cd ..
python main.py
```

---

## ✅ Результат

После этого:
- ✅ Нет конфликтов database locked
- ✅ Все 4 broadcaster'а работают параллельно
- ✅ Правильное распределение аккаунтов
- ✅ Оптимальная производительность

---

**Дата:** 2025-11-04  
**Статус:** Критически важно для работы!

