# Решение проблемы ротации лог-файлов на Windows

## 🔍 Проблема

На Windows при попытке ротации лог-файла возникает ошибка:

```
PermissionError: [WinError 32] The process cannot access the file because it is being used by another process
```

### Причины

1. **Файл заблокирован другим процессом**:
   - Другой экземпляр бота использует тот же файл
   - Файл открыт в текстовом редакторе
   - Антивирус сканирует файл
   - Другие процессы имеют открытый дескриптор файла

2. **Windows не позволяет переименовать открытый файл**:
   - В отличие от Linux/Unix, Windows блокирует переименование открытых файлов
   - `RotatingFileHandler` пытается переименовать файл при ротации
   - Это вызывает `PermissionError`

## ✅ Решение

Создан кастомный `WindowsSafeRotatingFileHandler`, который:

1. **Обрабатывает PermissionError** при ротации
2. **Продолжает работу** даже при ошибке ротации
3. **Пишет в текущий файл** вместо краша приложения
4. **Выводит предупреждение** в stderr вместо исключения

### Как это работает

```python
class WindowsSafeRotatingFileHandler(logging.handlers.RotatingFileHandler):
    def doRollover(self):
        try:
            super().doRollover()
        except (PermissionError, OSError) as e:
            # На Windows просто пропускаем ротацию
            # и продолжаем писать в текущий файл
            if sys.platform == 'win32':
                # Выводим предупреждение, но не крашим приложение
                print("⚠️  Не удалось выполнить ротацию...", file=sys.stderr)
            else:
                raise
```

## 🔧 Что делать при ошибке

### Вариант 1: Закрыть другие процессы

1. **Найти процессы, использующие файл**:
   ```powershell
   # В PowerShell
   Get-Process | Where-Object {$_.Path -like "*SendMessageBot*"}
   ```

2. **Закрыть другие экземпляры бота**:
   - Проверить, не запущено ли несколько экземпляров
   - Закрыть лишние процессы

3. **Закрыть текстовые редакторы**:
   - Если файл открыт в Notepad++, VS Code и т.д.

### Вариант 2: Перезапустить бота

```powershell
# Остановить бота
Stop-Service SendMessageBot
# Или через Task Manager

# Подождать несколько секунд
Start-Sleep -Seconds 5

# Запустить снова
Start-Service SendMessageBot
```

### Вариант 3: Ручная ротация

Если файл стал слишком большим:

```powershell
# Переименовать вручную (когда бот остановлен)
Rename-Item bot.log bot.log.old

# Запустить бот - он создаст новый файл
```

## 📊 Поведение после исправления

### До исправления:
- ❌ Приложение крашилось с `PermissionError`
- ❌ Логирование прекращалось
- ❌ Нужно было перезапускать бота

### После исправления:
- ✅ Приложение продолжает работать
- ✅ Логирование продолжается в текущий файл
- ✅ Выводится предупреждение в stderr
- ✅ Ротация пропускается, но работа не прерывается

## 💡 Рекомендации

1. **Использовать один экземпляр бота**:
   - Не запускать несколько копий одновременно
   - Использовать службу Windows для управления

2. **Не открывать лог-файл в редакторе**:
   - Использовать `tail` или просмотр после остановки бота
   - Или использовать `Get-Content -Wait` в PowerShell

3. **Настроить ротацию по времени** (опционально):
   - Можно использовать `TimedRotatingFileHandler` вместо `RotatingFileHandler`
   - Ротация будет происходить в определенное время, когда файл может быть свободен

4. **Мониторить размер файла**:
   - Если ротация не происходит, файл будет расти
   - Периодически проверять размер и при необходимости переименовывать вручную

## 🚀 После обновления

После применения исправления:

1. **Перезапустить бота**:
   ```powershell
   Restart-Service SendMessageBot
   ```

2. **Проверить логи**:
   ```powershell
   Get-Content bot.log -Tail 50
   ```

3. **Убедиться, что ошибок нет**:
   - Ошибки `PermissionError` больше не должны крашить приложение
   - При попытке ротации будет выводиться предупреждение

## 📝 Технические детали

### Почему это работает

1. **Обработка исключений**: `PermissionError` перехватывается и обрабатывается
2. **Продолжение работы**: Вместо краша приложение продолжает писать в текущий файл
3. **Предупреждение**: Пользователь видит предупреждение, но работа не прерывается
4. **Платформо-специфично**: На Linux/Unix ошибка пробрасывается дальше (там это не должно происходить)

### Ограничения

- Файл будет расти, если ротация не происходит
- Нужно периодически проверять размер файла
- При очень больших файлах может быть медленнее

