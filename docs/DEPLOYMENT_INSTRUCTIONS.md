# Инструкции по деплою в продакшен

## 📦 Подготовка к деплою

### 1. Проверка изменений

```bash
# Проверить статус
git status

# Просмотреть изменения
git diff

# Убедиться, что все файлы добавлены
git status
```

### 2. Создание коммита (уже выполнено)

```bash
git commit -m "feat: Добавлен GUS_B2C broadcaster и система координации"
```

### 3. Отправка в репозиторий

```bash
# Отправить изменения
git push origin master

# Или если используете другую ветку
git push origin <branch-name>
```

## 🚀 Деплой на сервер

### Вариант 1: Через SSH (если есть прямой доступ)

```bash
# Подключиться к серверу
ssh user@your-server.com

# Перейти в директорию проекта
cd /path/to/SendMessageBot

# Получить последние изменения
git pull origin master

# Проверить, что файлы обновились
ls -la core/coordinator.py
ls -la docs/BROADCASTER_COORDINATION.md

# Перезапустить бота (если используется systemd)
sudo systemctl restart SendMessageBot

# Или если используется screen/tmux
# Найти процесс и перезапустить
```

### Вариант 2: Через скрипт деплоя (если настроен)

```bash
# Запустить скрипт деплоя
./deploy.sh

# Или
bash scripts/deploy.sh
```

## ✅ Проверка после деплоя

### 1. Проверить логи

```bash
# Просмотреть логи
tail -f logs/bot.log

# Или если используется journalctl
journalctl -u SendMessageBot -f
```

### 2. Проверить, что broadcaster'ы запущены

В логах должны быть строки:
```
✅ GUS B2C Broadcaster создан (acc1_b2c/Яблочный Гусь): X чатов, Y сообщений, цикл: 2 часа, задержка: 120.0с
✅ Broadcaster 'GUS_B2C_Broadcaster' зарегистрирован в координаторе
```

### 3. Проверить координатор

В логах должны быть предупреждения о пересечениях чатов (если есть):
```
⚠️  ВНИМАНИЕ: Broadcaster 'GUS_B2C_Broadcaster' имеет пересекающиеся чаты:
   • С 'GUS_PRICE_Broadcaster': X общих чатов
```

### 4. Проверить работу

```bash
# Проверить статистику
python scripts/show_stats.py

# Или через скрипт
python scripts/manage_reports.py --status
```

## 🔧 Настройка сессий

### Создание файла сессии для GUS_B2C

```bash
# На сервере скопировать существующую сессию
cp sessions/acc1.session sessions/acc1_b2c.session

# Или авторизоваться заново при первом запуске
# Бот автоматически создаст файл сессии
```

## 📝 Чеклист перед деплоем

- [ ] Все изменения закоммичены
- [ ] Изменения отправлены в репозиторий
- [ ] Создан файл сессии `sessions/acc1_b2c.session` (или будет создан автоматически)
- [ ] Проверены настройки в `.env` (если нужны)
- [ ] Резервная копия текущей версии создана
- [ ] План отката подготовлен (если что-то пойдет не так)

## 🔄 Откат изменений (если нужно)

```bash
# На сервере
cd /path/to/SendMessageBot

# Откатить к предыдущему коммиту
git reset --hard HEAD~1

# Или к конкретному коммиту
git reset --hard <commit-hash>

# Перезапустить бота
sudo systemctl restart SendMessageBot
```

## 📊 Мониторинг после деплоя

1. **Проверить отчеты** (если настроены):
   - Отчеты должны приходить каждые 3 часа
   - Проверить, что новый broadcaster включен в статистику

2. **Проверить ошибки**:
   - Следить за логами первые несколько часов
   - Проверить, что нет новых типов ошибок

3. **Проверить производительность**:
   - Убедиться, что координатор работает корректно
   - Проверить, что нет конфликтов между broadcaster'ами

## 🆘 Решение проблем

### Проблема: Бот не запускается

```bash
# Проверить логи ошибок
tail -100 logs/bot.log | grep ERROR

# Проверить синтаксис Python
python -m py_compile main.py
python -m py_compile core/coordinator.py
python -m py_compile core/broadcaster.py
```

### Проблема: Координатор не работает

```bash
# Проверить, что файл существует
ls -la core/coordinator.py

# Проверить импорты
python -c "from core.coordinator import get_coordinator; print('OK')"
```

### Проблема: Сессия не найдена

```bash
# Проверить файлы сессий
ls -la sessions/

# Создать недостающую сессию
cp sessions/acc1.session sessions/acc1_b2c.session
```

