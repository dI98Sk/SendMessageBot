# 🏗️ Архитектура микросервисов

**Дата:** 2025-12-10  
**Версия:** 2.0

---

## 📋 Концепция

Проект состоит из двух независимых микросервисов:

1. **Broadcaster Service** - рассылка сообщений в Telegram
2. **Google Sheets Updater Service** - автоматическое обновление Google таблиц

### Принципы разделения:

- ✅ **Независимость:** Сервисы не обмениваются данными напрямую
- ✅ **Единый репозиторий:** Оба сервиса в одном проекте
- ✅ **Общие утилиты:** Общий Google Sheets клиент (но без обмена данными)
- ✅ **Отдельные конфигурации:** Каждый сервис имеет свою конфигурацию
- ✅ **Отдельные логи:** Каждый сервис логирует в свой файл

---

## 📁 Структура проекта

```
SendMessageBot/
├── broadcaster/                    # Сервис рассылки (существующий)
│   ├── main.py                    # Точка входа
│   ├── core/                      # Ядро broadcaster'ов
│   │   ├── broadcaster.py
│   │   ├── coordinator.py
│   │   └── ...
│   ├── config/                     # Конфигурация broadcaster
│   │   ├── settings.py
│   │   ├── targets.py
│   │   └── ...
│   ├── monitoring/                 # Мониторинг и отчеты
│   └── utils/                      # Утилиты (если специфичны для broadcaster)
│
├── google_sheets_updater/          # Сервис обновления таблиц (новый)
│   ├── main.py                     # Точка входа
│   ├── updater/                    # Логика обновления
│   │   ├── sheet_updater.py        # Основной класс обновления
│   │   ├── data_sources.py         # Источники данных
│   │   └── validators.py           # Валидация данных
│   ├── config/                     # Конфигурация updater
│   │   ├── settings.py
│   │   └── sheets_config.py
│   └── utils/                      # Утилиты (если специфичны для updater)
│
├── shared/                         # Общие компоненты
│   ├── google_sheets/              # Google Sheets клиент
│   │   ├── __init__.py
│   │   ├── client.py               # Базовый клиент
│   │   └── fetcher.py              # Для чтения (broadcaster)
│   └── logger.py                   # Общий логгер (опционально)
│
├── requirements.txt                # Общие зависимости
├── requirements-broadcaster.txt    # Доп. зависимости для broadcaster
├── requirements-updater.txt        # Доп. зависимости для updater
│
├── scripts/
│   ├── start_broadcaster.py        # Запуск broadcaster
│   ├── start_updater.py            # Запуск updater
│   └── start_all.py                # Запуск обоих (опционально)
│
├── .env.broadcaster                # Конфигурация broadcaster
├── .env.updater                    # Конфигурация updater
│
├── README.md
└── docs/
    └── MICROSERVICES_ARCHITECTURE.md
```

---

## 🔧 Реализация

### 1. Структура директорий

**Broadcaster Service:**
- Остается в корне проекта (для обратной совместимости)
- Или перемещается в `broadcaster/` (рекомендуется)

**Google Sheets Updater Service:**
- Новая директория `google_sheets_updater/`
- Полностью независимая структура

### 2. Общие компоненты

**Google Sheets Client:**
- Выносится в `shared/google_sheets/`
- Используется обоими сервисами
- Но без обмена данными между ними

### 3. Конфигурация

**Разделение:**
- `.env.broadcaster` - для сервиса рассылки
- `.env.updater` - для сервиса обновления
- Или один `.env` с префиксами: `BROADCASTER_*` и `UPDATER_*`

### 4. Запуск

**Независимый запуск:**
```bash
# Запуск broadcaster
python broadcaster/main.py
# или
python -m broadcaster.main

# Запуск updater
python google_sheets_updater/main.py
# или
python -m google_sheets_updater.main
```

**Скрипты запуска:**
```bash
# Windows
scripts\start_broadcaster.bat
scripts\start_updater.bat

# Linux/Mac
scripts/start_broadcaster.sh
scripts/start_updater.sh
```

---

## 📊 Google Sheets Updater Service

### Функциональность:

1. **Чтение данных из источников:**
   - API внешних сервисов
   - Базы данных
   - Файлы
   - Другие Google таблицы

2. **Обработка данных:**
   - Валидация
   - Трансформация
   - Форматирование

3. **Запись в Google таблицы:**
   - Обновление существующих таблиц
   - Создание новых листов (если нужно)
   - Логирование изменений

4. **Мониторинг:**
   - Логирование операций
   - Отчеты об обновлениях
   - Обработка ошибок

### Конфигурация:

```python
# google_sheets_updater/config/settings.py
@dataclass
class UpdaterConfig:
    # Google Sheets
    credentials_file: str
    target_sheets: Dict[str, str]  # {sheet_name: sheet_url}
    
    # Источники данных
    data_sources: List[Dict[str, Any]]
    
    # Интервал обновления
    update_interval_seconds: int = 3600  # 1 час
    
    # Логирование
    log_file: str = "logs/updater.log"
    log_level: str = "INFO"
```

---

## 🔒 Изоляция сервисов

### Принципы:

1. **Нет прямого обмена данными:**
   - Сервисы не импортируют друг друга
   - Нет общих переменных состояния
   - Нет общих файлов данных

2. **Общие компоненты:**
   - Только read-only утилиты (Google Sheets клиент)
   - Без состояния между сервисами

3. **Конфигурация:**
   - Отдельные файлы конфигурации
   - Отдельные переменные окружения

4. **Логирование:**
   - Отдельные файлы логов
   - Отдельные префиксы в логах

---

## 🚀 Миграция существующего кода

### Шаг 1: Создать структуру

```bash
mkdir -p google_sheets_updater/updater
mkdir -p google_sheets_updater/config
mkdir -p shared/google_sheets
```

### Шаг 2: Вынести Google Sheets клиент

```bash
# Переместить utils/google_sheets.py в shared/google_sheets/client.py
# Обновить импорты в broadcaster
```

### Шаг 3: Создать новый сервис

```bash
# Создать google_sheets_updater/main.py
# Создать google_sheets_updater/updater/sheet_updater.py
```

### Шаг 4: Обновить документацию

```bash
# Обновить README.md
# Создать документацию для нового сервиса
```

---

## 📝 Примеры использования

### Запуск только Broadcaster:

```bash
python broadcaster/main.py
```

### Запуск только Updater:

```bash
python google_sheets_updater/main.py
```

### Запуск обоих (разные процессы):

```bash
# Windows
start python broadcaster/main.py
start python google_sheets_updater/main.py

# Linux/Mac
python broadcaster/main.py &
python google_sheets_updater/main.py &
```

---

## ✅ Преимущества архитектуры

1. **Независимость:**
   - Каждый сервис можно запускать/останавливать отдельно
   - Обновления не влияют друг на друга

2. **Масштабируемость:**
   - Легко добавить новые сервисы
   - Легко разделить на отдельные контейнеры (Docker)

3. **Поддерживаемость:**
   - Четкое разделение ответственности
   - Легче тестировать и отлаживать

4. **Гибкость:**
   - Можно использовать разные версии зависимостей
   - Можно развертывать на разных серверах

---

## 🔗 Связанные документы

- [README.md](../README.md) - общая документация проекта
- [DEPLOYMENT_INSTRUCTIONS.md](DEPLOYMENT_INSTRUCTIONS.md) - инструкции по деплою

---

**Дата создания:** 2025-12-10  
**Версия:** 1.0

