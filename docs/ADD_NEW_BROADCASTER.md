# Инструкция по добавлению нового broadcaster'а

## 📋 Обзор

Эта инструкция поможет добавить новый broadcaster в уже работающую систему без остановки существующих.

## 📝 Особенности GUS_B2C_Broadcaster

**GUS_B2C_Broadcaster** использует сообщения из **двух таблиц Google Sheets**:
- `BUY_SELL_PRICE_GUS_SHEET_URL` - прайсы GUS
- `ADS_GUS_SHEET_URL` - реклама GUS

Сообщения автоматически объединяются: `gus_messages + gus_ads_messages`

При автообновлении из Google Sheets GUS_B2C автоматически получает обновления из обеих таблиц.

## 🎯 Шаги добавления

### Шаг 1: Определить параметры нового broadcaster'а

Определите:
- **Название**: например, `NEW_BROADCASTER`
- **Аккаунт**: какой Telegram аккаунт использовать (acc1 или acc2)
- **Цели**: список chat_id для рассылки
- **Сообщения**: список сообщений для отправки
- **Цикл**: интервал между циклами (в секундах)
- **Задержка между чатами**: задержка между отправками (в секундах)
- **Смещение старта**: смещение времени старта (в секундах)

### Шаг 2: Добавить цели в `config/targets.py`

```python
# В config/targets.py добавить новый список
NEW_TARGET = [
    -1001234567890,
    -1001234567891,
    # ... остальные chat_id
]
```

### Шаг 3: Обновить `config/settings.py`

#### 3.1. Добавить поле в `AppConfig`

```python
# В классе AppConfig добавить:
targets_new: List[int] = field(default_factory=list)  # Новые цели
new_messages: List[str] = field(default_factory=list)  # Новые сообщения
```

#### 3.2. Добавить импорт в `load_config()`

```python
# В методе load_config() добавить импорт:
from .targets import NEW_TARGET
from .messages_new import MESSAGES_NEW  # или откуда берете сообщения
```

#### 3.3. Добавить в конфигурацию

```python
# В методе load_config() добавить в AppConfig:
targets_new=NEW_TARGET,
new_messages=MESSAGES_NEW,
```

### Шаг 4: Добавить broadcaster в `main.py`

В методе `_create_broadcasters()` добавить:

```python
# ========================================
# НОВЫЙ BROADCASTER - описание
# ========================================

NEW_CYCLE_DELAY = 2 * 60 * 60  # 2 часа = 7200 секунд
NEW_DELAY_BETWEEN_CHATS = 120.0  # 2 минуты между чатами
NEW_START_OFFSET = 600  # 10 минут смещение старта

# Новый broadcaster
new_broadcaster = EnhancedBroadcaster(
    config=self.config,
    name="NEW_Broadcaster",
    targets=self.config.targets_new,
    messages=self.config.new_messages,
    session_name="sessions/acc1_new",  # или acc2_new
    cycle_delay=NEW_CYCLE_DELAY,
    delay_between_chats=NEW_DELAY_BETWEEN_CHATS,
    start_offset_seconds=NEW_START_OFFSET
)
self.broadcasters.append(new_broadcaster)
print(f"✅ NEW Broadcaster создан (acc1_new): {len(self.config.targets_new)} чатов, {len(self.config.new_messages)} сообщений, цикл: {NEW_CYCLE_DELAY/60} мин")
```

### Шаг 5: Создать файл сессии

```bash
# На сервере скопировать существующую сессию
cp sessions/acc1.session sessions/acc1_new.session

# Или создать пустой файл (авторизация произойдет при первом запуске)
touch sessions/acc1_new.session
```

## 🔧 Настройка координации

### Проверка пересечений чатов

Система автоматически проверит пересечения при запуске. Если есть пересечения:

1. **Разделить чаты** (рекомендуется):
   - Использовать разные списки чатов
   - Избегать дублирования

2. **Увеличить задержки**:
   - Увеличить `delay_between_chats`
   - Увеличить `min_interval_per_chat` в конфигурации

3. **Использовать разные аккаунты**:
   - Если возможно, использовать другой аккаунт

### Настройка задержек

Рекомендации по задержкам:

| Цикл | Задержка между чатами | Смещение старта |
|------|----------------------|-----------------|
| 20 мин | 90-120с | 0-300с |
| 1 час | 90-120с | 300-600с |
| 2 часа | 120-180с | 600-900с |
| 4+ часа | 180-300с | 900-1800с |

## 🚀 Деплой без остановки

### Вариант 1: Горячий деплой (рекомендуется)

1. **Добавить изменения в код** (локально)
2. **Протестировать локально** (если возможно)
3. **Закоммитить и отправить**:
   ```bash
   git add .
   git commit -m "feat: Добавлен NEW_Broadcaster"
   git push origin master
   ```
4. **На сервере обновить код**:
   ```bash
   cd /path/to/SendMessageBot
   git pull origin master
   ```
5. **Перезапустить бота**:
   ```bash
   # Если используется systemd
   sudo systemctl restart SendMessageBot
   
   # Или если используется screen/tmux
   # Найти процесс и перезапустить
   ```

### Вариант 2: Постепенный деплой

1. **Добавить код** (без запуска нового broadcaster'а)
2. **Деплой на сервер**
3. **Проверить работу существующих broadcaster'ов**
4. **Раскомментировать новый broadcaster** в коде
5. **Еще один деплой**

## ✅ Проверка после добавления

### 1. Проверить логи

```bash
tail -f logs/bot.log | grep "NEW_Broadcaster"
```

Должны быть строки:
```
✅ NEW Broadcaster создан (acc1_new): X чатов, Y сообщений, цикл: Z мин
✅ Broadcaster 'NEW_Broadcaster' зарегистрирован в координаторе
```

### 2. Проверить координатор

В логах должны быть предупреждения (если есть пересечения):
```
⚠️  ВНИМАНИЕ: Broadcaster 'NEW_Broadcaster' имеет пересекающиеся чаты:
   • С 'GUS_PRICE_Broadcaster': X общих чатов
```

### 3. Проверить статистику

```bash
python scripts/show_stats.py
```

Новый broadcaster должен появиться в статистике.

### 4. Проверить работу

Подождать один цикл и проверить:
- Отправляются ли сообщения
- Нет ли ошибок
- Работает ли координация

## 📊 Пример полного добавления

### Пример: Добавление TEST_Broadcaster

**1. config/targets.py:**
```python
TEST_TARGET = [
    -1001234567890,
    -1001234567891,
]
```

**2. config/settings.py:**
```python
# В AppConfig:
targets_test: List[int] = field(default_factory=list)
test_messages: List[str] = field(default_factory=list)

# В load_config():
from .targets import TEST_TARGET
from .messages_test import MESSAGES_TEST

# В AppConfig():
targets_test=TEST_TARGET,
test_messages=MESSAGES_TEST,
```

**3. main.py:**
```python
# В _create_broadcasters():
TEST_CYCLE_DELAY = 60 * 60  # 1 час
TEST_DELAY_BETWEEN_CHATS = 90.0
TEST_START_OFFSET = 300

test_broadcaster = EnhancedBroadcaster(
    config=self.config,
    name="TEST_Broadcaster",
    targets=self.config.targets_test,
    messages=self.config.test_messages,
    session_name="sessions/acc1_test",
    cycle_delay=TEST_CYCLE_DELAY,
    delay_between_chats=TEST_DELAY_BETWEEN_CHATS,
    start_offset_seconds=TEST_START_OFFSET
)
self.broadcasters.append(test_broadcaster)
print(f"✅ TEST Broadcaster создан: {len(self.config.targets_test)} чатов, {len(self.config.test_messages)} сообщений")
```

**4. Создать сессию:**
```bash
cp sessions/acc1.session sessions/acc1_test.session
```

## 🆘 Решение проблем

### Проблема: Broadcaster не запускается

- Проверить, что файл сессии существует
- Проверить логи на ошибки
- Проверить, что targets и messages не пустые

### Проблема: Конфликты с другими broadcaster'ами

- Увеличить задержки
- Разделить чаты
- Использовать другой аккаунт

### Проблема: Слишком много ошибок

- Увеличить `delay_between_chats`
- Увеличить `min_interval_per_chat`
- Проверить валидность chat_id

## 📝 Чеклист

- [ ] Добавлены цели в `targets.py`
- [ ] Обновлен `settings.py` (поля, импорты, конфигурация)
- [ ] Добавлен broadcaster в `main.py`
- [ ] Создан файл сессии
- [ ] Протестировано локально (если возможно)
- [ ] Закоммичено и отправлено в репозиторий
- [ ] Деплой на сервер выполнен
- [ ] Проверены логи после запуска
- [ ] Проверена статистика
- [ ] Проверена работа координатора

## 💡 Рекомендации

1. **Всегда используйте уникальные имена** для broadcaster'ов
2. **Используйте уникальные файлы сессий** для каждого broadcaster'а
3. **Проверяйте пересечения чатов** перед добавлением
4. **Начинайте с консервативных задержек** (можно уменьшить позже)
5. **Мониторьте логи** первые несколько циклов после добавления
6. **Используйте координатор** для предотвращения конфликтов

