# 📊 GUS_B2C_MIDSLOW_Broadcaster

## 📋 Обзор

Новый broadcaster для розничных целей B2C_MIDSLOW с более редкой частотой отправки.

## ⚙️ Конфигурация

### Параметры:

- **Название:** `GUS_B2C_MIDSLOW_Broadcaster`
- **Аккаунт:** acc1 (Яблочный Гусь)
- **Сессия:** `sessions/acc1_b2c_midslow.session`
- **Чаты:** 52 уникальных чата (из B2C_TARGET_MIDSLOW)
- **Сообщения:** Объединенные из двух таблиц:
  - Прайсы GUS (`BUY_SELL_PRICE_GUS_SHEET_URL`)
  - Реклама GUS (`ADS_GUS_SHEET_URL`)
- **Логика:** Одно случайное сообщение на чат за цикл
- **Цикл:** 2.67 часа (160 минут)
- **Задержка между чатами:** 60 секунд (1 минута)
- **Смещение старта:** 600 секунд (10 минут)

---

## ⏱️ Расчет времени цикла

### Время выполнения:

```
Время цикла = 52 чата × 60 секунд = 3,120 секунд
            = 52 минуты
            = 0.87 часа
```

### Настроенный цикл:

```
Цикл = 2.67 часа (160 минут)
Запас времени = 160 - 52 = 108 минут ✅
```

**Статус:** ✅ Время выполнения вписывается в настроенный цикл с большим запасом.

---

## 📈 Циклы в сутки

### Расчет:

```
Циклов в сутки = (24 часа × 60 минут) / 160 минут
               = 1440 / 160
               = 9 циклов в сутки
```

**Результат:** ✅ **9 циклов в сутки** (в диапазоне 8-10)

---

## 🎯 Общая статистика всех broadcaster'ов

| Broadcaster | Чаты | Цикл | Циклов/день |
|------------|------|------|-------------|
| AAA_PRICE | 27 | 30 мин | 48 |
| GUS_PRICE | 27 | 30 мин | 48 |
| AAA_ADS | 16 | 50 мин | 28.8 |
| GUS_ADS | 16 | 50 мин | 28.8 |
| GUS_B2C | 73 | 90 мин | 16 |
| **GUS_B2C_MIDSLOW** | **52** | **160 мин** | **9** |

**Итого циклов за сутки:** 96 + 57.6 + 16 + 9 = **178.6 циклов**

---

## 🔧 Согласованная работа

### Координация с другими broadcaster'ами:

1. **Глобальный координатор:**
   - Регистрируется в `BroadcasterCoordinator`
   - Проверяет глобальные ограничения перед отправкой
   - Получает блокировку чата перед отправкой

2. **Смещение времени старта:**
   - 10 минут смещение (600 секунд)
   - Распределяет нагрузку во времени
   - Избегает конфликтов с другими broadcaster'ами

3. **Уникальная сессия:**
   - `sessions/acc1_b2c_midslow.session`
   - Предотвращает `database locked` ошибки
   - Изолированная работа от других broadcaster'ов

---

## 📝 Настройка

### 1. Файл сессии

Создается автоматически при запуске `scripts/setup_accounts.py`:

```bash
python scripts/setup_accounts.py
```

Или вручную:

```bash
cp sessions/acc1.session sessions/acc1_b2c_midslow.session
```

### 2. Конфигурация

Все настройки в `main.py`:

```python
B2C_MIDSLOW_CYCLE_DELAY = int(2.67 * 60 * 60)  # 2.67 часа = 9612 секунд
B2C_MIDSLOW_DELAY_BETWEEN_CHATS = 60.0  # 1 минута
B2C_MIDSLOW_START_OFFSET = 600  # 10 минут
```

---

## 🚀 Запуск

Broadcaster создается автоматически при запуске `main.py`, если в `B2C_TARGET_MIDSLOW` есть чаты.

### Проверка создания:

При запуске вы увидите:

```
✅ GUS B2C MIDSLOW Broadcaster создан (acc1_b2c_midslow/Яблочный Гусь): 
   52 чатов, 29 сообщений (случайное на чат) 
   (прайсы: 13, реклама: 16), 
   цикл: 2.67 часа (~9 циклов/день), задержка: 60.0с
```

---

## 📊 Преимущества

1. ✅ **Редкая частота** - не перегружает чаты
2. ✅ **Разнообразие** - каждое сообщение отправляется за несколько циклов
3. ✅ **Согласованность** - работает в координации с другими broadcaster'ами
4. ✅ **Оптимальный цикл** - 9 циклов в сутки (в диапазоне 8-10)
5. ✅ **Большой запас времени** - 108 минут запас для обработки ошибок

---

## 🔍 Мониторинг

### Логи:

```
🔄 [GUS_B2C_MIDSLOW_Broadcaster] Начинаем цикл рассылки | 
   Целевых чатов: 52 | 
   Сообщений: 29 | 
   Текущая задержка: 60.0с
```

### Статистика:

- Отправлено сообщений
- Ошибки по типам
- Проблемные чаты
- Время выполнения цикла

---

## 💡 Рекомендации

1. **Мониторинг:** Следите за временем выполнения циклов
2. **Оптимизация:** При необходимости можно скорректировать цикл
3. **Координация:** Убедитесь, что нет конфликтов с другими broadcaster'ами
4. **Валидация:** Регулярно проверяйте валидность chat_id

---

## 📚 Связанная документация

- [ALL_BROADCASTERS_ANALYSIS.md](ALL_BROADCASTERS_ANALYSIS.md) - анализ всех broadcaster'ов
- [BROADCASTER_COORDINATION.md](BROADCASTER_COORDINATION.md) - система координации
- [ADD_NEW_BROADCASTER.md](ADD_NEW_BROADCASTER.md) - как добавить новый broadcaster

