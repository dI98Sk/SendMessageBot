# 🧪 Руководство по тестированию и production запуску

## 📊 Две версии приложения

### 1. `main.py` - PRODUCTION версия

**Используется для работы с реальными чатами**

- **Прайсы**: `PRICE_TARGET` (22 чата)
- **Реклама**: `ADS_TARGET` (11 чатов)
- **Scheduling**: Включен (запуск в 6:00)
- **Тихий час**: Включен (00:00-07:00)

```bash
python main.py
```

### 2. `main_test.py` - ТЕСТОВАЯ версия

**Полный дубликат main.py, но с тестовыми чатами**

- **Прайсы**: `TEST_TARGETS` (2 чата)
- **Реклама**: `TEST_TARGETS_ADS` (2 чата)
- **Scheduling**: Автоматически ОТКЛЮЧЕН
- **Тихий час**: Автоматически ОТКЛЮЧЕН
- **Запуск**: Немедленный

```bash
python main_test.py
```

## 🎯 Когда использовать каждую версию

### Production (`main.py`)
✅ Используйте когда:
- Готовы к полноценному запуску
- Сообщения обновлены и проверены
- Все тесты пройдены
- Работа в production режиме

### Тестовая (`main_test.py`)
✅ Используйте когда:
- Тестируете новые сообщения
- Проверяете новую функциональность
- Отлаживаете broadcaster'ы
- Не хотите спамить production чаты

## 📋 Сравнительная таблица

| Параметр | main.py | main_test.py |
|----------|---------|--------------|
| Прайсы AAA | 22 чата (PRICE_TARGET) | 2 чата (TEST_TARGETS) |
| Прайсы GUS | 22 чата (PRICE_TARGET) | 2 чата (TEST_TARGETS) |
| Реклама AAA | 11 чатов (ADS_TARGET) | 2 чата (TEST_TARGETS_ADS) |
| Реклама GUS | 11 чатов (ADS_TARGET) | 2 чата (TEST_TARGETS_ADS) |
| Время запуска | 6:00 (по настройке) | Немедленно |
| Тихий час | 00:00-07:00 | Отключен |
| Логи | bot.log | bot.log (с префиксом 🧪) |
| Отчеты | Обычные | С префиксом "ТЕСТОВЫЙ" |

## 🚀 Типичный рабочий процесс

### 1. Обновление сообщений

```bash
# Обновить прайсы
python scripts/update_messages.py

# Обновить рекламу
python scripts/update_ads_messages.py
```

### 2. Тестирование

```bash
# Быстрый тест конфигурации
python test_new_broadcasters.py

# Полный тест с ТЕСТОВЫМИ чатами
python main_test.py
```

Работает 3-5 минут, проверьте сообщения в тестовых чатах.

### 3. Production запуск

```bash
# Если всё ОК, запускаем на production
python main.py
```

## 🧪 Тестовые чаты

### TEST_TARGETS (для прайсов)
```python
TEST_TARGETS = [
    -1002679672234,  # ТЕСТ Рассылок 2
    -1002805990284,  # ТЕСТ РАССЫЛОК 1
]
```

### TEST_TARGETS_ADS (для рекламы)
```python
TEST_TARGETS_ADS = [
    -5042413579,  # ТЕСТ РЕКЛАМЫ 2
    -4918385916,  # ТЕСТ РЕКЛАМЫ 1
]
```

## 📊 Мониторинг

### Логи

```bash
# Все логи
tail -f bot.log

# Только тестовые
tail -f bot.log | grep "🧪"

# Только production
tail -f bot.log | grep -v "🧪"
```

### Статистика

```bash
# В реальном времени
python watch_stats.py

# Разовая
python show_stats.py
```

## ⚠️ Важные замечания

### main.py (Production)

1. **Запускается по расписанию** - broadcaster'ы ждут 6:00 утра
2. **Тихий час** - не работает с 00:00 до 07:00
3. **Все ограничения** - полные задержки и ограничения
4. **Большая нагрузка** - 22+11 = 33 чата

### main_test.py (Тестовая)

1. **Запуск немедленный** - не ждет расписания
2. **Нет тихого часа** - работает всегда
3. **Меньше чатов** - быстрее проверка
4. **Безопасно** - не спамит production

## 🔧 Переключение между версиями

### Из тестовой в production

Если тест прошел успешно:
```bash
# Ctrl+C в main_test.py
# Затем запустить
python main.py
```

### Откат на тестовую

Если что-то пошло не так:
```bash
# Ctrl+C в main.py
# Проверить и исправить
# Протестировать
python main_test.py
```

## 📝 Чек-лист перед production

Перед запуском `main.py` убедитесь:

- [ ] Сообщения обновлены из Google Sheets
- [ ] Протестировано на `main_test.py`
- [ ] Проверены сообщения в тестовых чатах
- [ ] Нет ошибок в логах
- [ ] Система отчетов работает
- [ ] `.env` настроен правильно
- [ ] Все 4 broadcaster'а создаются корректно

## 🎯 Примеры использования

### Пример 1: Тест новых рекламных сообщений

```bash
# 1. Обновить рекламу
python scripts/update_ads_messages.py

# 2. Протестировать
python main_test.py
# Подождать 2-3 минуты, проверить TEST_TARGETS_ADS

# 3. Если ОК - запустить production
python main.py
```

### Пример 2: Тест новых прайсов

```bash
# 1. Обновить прайсы
python scripts/update_messages.py

# 2. Протестировать
python main_test.py
# Проверить TEST_TARGETS

# 3. Production
python main.py
```

### Пример 3: Полный тест всего

```bash
# 1. Обновить всё
python scripts/update_messages.py && python scripts/update_ads_messages.py

# 2. Быстрая проверка конфигурации
python test_new_broadcasters.py

# 3. Полный тест
python main_test.py
# Ждать 5 минут, проверить все тестовые чаты

# 4. Если всё ОК - production
python main.py
```

## 🆘 Решение проблем

### main_test.py не запускается

```bash
# Проверить конфигурацию
python test_new_broadcasters.py

# Проверить тестовые чаты
cat config/targets.py | grep TEST_TARGETS
```

### Сообщения не отправляются в main.py

Возможно, ждет времени запуска (6:00). Проверьте логи:
```bash
tail -f bot.log | grep "ждём до"
```

### Нужно срочно запустить main.py без ожидания

Временно отключите scheduling в `.env`:
```bash
ENABLE_SCHEDULING=false
ENABLE_QUIET_HOURS=false
```

Но для постоянной работы верните обратно!

---

**Готово! Теперь у вас есть безопасный способ тестирования перед production запуском! 🚀**

*Дата: 2025-11-04*


