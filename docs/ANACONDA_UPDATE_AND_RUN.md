# 📥 Инструкция: Обновление и запуск проекта в Anaconda Prompt

Инструкция для обновления кода из git и запуска проекта в уже существующей директории.

---

## 🚀 Быстрое обновление и запуск

### Вариант 1: Если виртуальное окружение уже создано

```cmd
# 1. Перейти в директорию проекта
cd C:\Users\Administrator\PycharmProjects\SendMessageBot_v2

# 2. Обновить код из git
git pull origin master

# 3. Активировать виртуальное окружение
venv\Scripts\activate

# 4. Обновить зависимости (если requirements.txt изменился)
pip install -r requirements.txt --upgrade

# 5. Запустить проект
python main.py
```

---

### Вариант 2: Если виртуальное окружение НЕ создано или нужно пересоздать

```cmd
# 1. Перейти в директорию проекта
cd C:\Users\Administrator\PycharmProjects\SendMessageBot_v2

# 2. Обновить код из git
git pull origin master

# 3. Создать виртуальное окружение (если его нет)
python -m venv venv

# 4. Активировать виртуальное окружение
venv\Scripts\activate

# 5. Обновить pip
python -m pip install --upgrade pip

# 6. Установить зависимости
pip install -r requirements.txt

# 7. Настроить аккаунты (если нужно)
python scripts\setup_accounts.py

# 8. Запустить проект
python main.py
```

---

## 📋 Пошаговая инструкция с объяснениями

### Шаг 1: Открыть Anaconda Prompt

1. Нажмите `Win + R`
2. Введите `anaconda-prompt` или найдите "Anaconda Prompt" в меню Пуск
3. Откройте Anaconda Prompt

### Шаг 2: Перейти в директорию проекта

```cmd
cd C:\Users\Administrator\PycharmProjects\SendMessageBot_v2
```

**Примечание:** Замените путь на актуальный путь к вашему проекту.

### Шаг 3: Проверить статус git

```cmd
git status
```

Это покажет:
- Есть ли незакоммиченные изменения
- На какой ветке вы находитесь
- Есть ли новые коммиты в удаленном репозитории

### Шаг 4: Обновить код из git

```cmd
git pull origin master
```

**Если возникнут конфликты:**
```cmd
# Посмотреть конфликты
git status

# Если нужно отменить локальные изменения и взять версию с сервера
git stash
git pull origin master
git stash pop
```

### Шаг 5: Активировать виртуальное окружение

```cmd
venv\Scripts\activate
```

После активации в начале строки появится `(venv)`.

### Шаг 6: Обновить зависимости

```cmd
pip install -r requirements.txt --upgrade
```

Это обновит все пакеты до версий, указанных в `requirements.txt`.

### Шаг 7: Проверить .env файл

Убедитесь, что файл `.env` существует и содержит все необходимые переменные:

```cmd
# Проверить наличие файла
dir .env

# Если файла нет, скопируйте из .env.example
copy .env.example .env
```

**Важные переменные:**
- `API_ID` и `API_HASH` (Telegram)
- `PHONE_NUMBER` (номер телефона для авторизации)
- Google Sheets URLs
- `TELEGRAM_REPORT_CHAT_ID` (для отчетов)

### Шаг 8: Запустить проект

```cmd
python main.py
```

---

## 🔄 Ежедневное обновление (быстрая команда)

Если проект уже настроен и нужно только обновить код:

```cmd
cd C:\Users\Administrator\PycharmProjects\SendMessageBot_v2 && git pull origin master && venv\Scripts\activate && python main.py
```

---

## ⚠️ Решение проблем

### Проблема: "git pull" выдает ошибку "Your local changes would be overwritten"

**Решение:**
```cmd
# Сохранить локальные изменения
git stash

# Обновить код
git pull origin master

# Применить сохраненные изменения обратно
git stash pop
```

### Проблема: "venv\Scripts\activate" не работает

**Решение:**
```cmd
# Проверить, существует ли venv
dir venv

# Если нет, создать заново
python -m venv venv
venv\Scripts\activate
```

### Проблема: "ModuleNotFoundError" при запуске

**Решение:**
```cmd
# Убедиться, что venv активирован (должно быть (venv) в начале строки)
# Переустановить зависимости
pip install -r requirements.txt --force-reinstall
```

### Проблема: "PermissionError" при запуске

**Решение:**
```cmd
# Запустить Anaconda Prompt от имени администратора
# Или проверить права доступа к файлам
```

### Проблема: Конфликты при git pull

**Решение:**
```cmd
# Посмотреть конфликты
git status

# Если нужно взять версию с сервера (удалить локальные изменения)
git reset --hard origin/master
git pull origin master

# ВНИМАНИЕ: Это удалит все локальные изменения!
```

---

## 📝 Проверка после обновления

После обновления проверьте:

1. **Логи запуска:**
   - Должны быть сообщения о запуске всех 6 broadcaster'ов
   - Не должно быть критических ошибок

2. **Файлы сессий:**
   ```cmd
   dir sessions\*.session
   ```
   Должны быть файлы для всех broadcaster'ов:
   - `acc1.session`, `acc2.session`
   - `acc1_price.session`, `acc2_price.session`
   - `acc1_ads.session`, `acc2_ads.session`
   - `acc1_b2c.session`, `acc1_b2c_midslow.session`

3. **Конфигурация:**
   - Проверьте, что `config/targets.py` содержит актуальные списки чатов
   - Проверьте, что `.env` содержит все необходимые переменные

---

## 🛑 Остановка проекта

Для остановки проекта нажмите:
```
Ctrl + C
```

Проект корректно завершит работу всех broadcaster'ов и сохранит состояние.

---

## 📊 Мониторинг работы

После запуска проверьте:

1. **Логи в консоли:**
   - Сообщения о запуске broadcaster'ов
   - Сообщения о начале циклов
   - Отчеты каждые 3 часа

2. **Файл логов:**
   ```cmd
   type bot.log
   ```
   Или откройте `bot.log` в текстовом редакторе.

3. **Отчеты в Telegram:**
   - Проверьте, что отчеты приходят каждые 3 часа в указанный чат

---

## 🔗 Полезные ссылки

- [README.md](../README.md) - Основная документация
- [SETUP_GUIDE.md](SETUP_GUIDE.md) - Полная инструкция по настройке
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - Решение проблем
- [scripts/README_START_WINDOWS.md](../scripts/README_START_WINDOWS.md) - Скрипты для Windows

---

**Дата создания:** 2025-01-16  
**Версия:** 1.0

