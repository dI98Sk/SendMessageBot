# ⚙️ Оптимизированная конфигурация циклов

## 🎯 Цель

**130-150 циклов за сутки (24 часа)** для всех broadcaster'ов вместе.

## 📊 Текущая конфигурация

### Все Broadcaster'ы используют логику одного случайного сообщения

| Broadcaster | Чаты | Сообщений | Цикл | Задержка | Циклов/день | Всего циклов |
|------------|------|-----------|------|----------|-------------|--------------|
| **AAA_PRICE** | 27 | ~30 | 30 мин | 60с | 48 | 96 |
| **GUS_PRICE** | 27 | ~30 | 30 мин | 60с | 48 | - |
| **AAA_ADS** | 16 | ~20 | 50 мин | 60с | 28.8 | 57.6 |
| **GUS_ADS** | 16 | ~20 | 50 мин | 60с | 28.8 | - |
| **GUS_B2C** | 66 | 29 | 1.5 часа | 60с | 16 | 16 |

**Итого: 96 + 57.6 + 16 = 169.6 циклов за сутки**

⚠️ **Немного больше целевого диапазона (130-150), но близко.**

---

## 📈 Расчет циклов

### Формула:
```
Циклов в сутки = (24 часа × 60 минут) / Цикл в минутах
```

### Детальный расчет:

**PRICE broadcaster'ы (2 шт):**
- Цикл: 30 минут
- Циклов в сутки на broadcaster: 1440 / 30 = **48 циклов**
- Всего: 48 × 2 = **96 циклов**

**ADS broadcaster'ы (2 шт):**
- Цикл: 50 минут
- Циклов в сутки на broadcaster: 1440 / 50 = **28.8 циклов**
- Всего: 28.8 × 2 = **57.6 циклов**

**B2C broadcaster (1 шт):**
- Цикл: 90 минут (1.5 часа)
- Циклов в сутки: 1440 / 90 = **16 циклов**

**Общий итог:**
- 96 + 57.6 + 16 = **169.6 циклов за сутки**

---

## ⚙️ Параметры конфигурации

### PRICE Broadcaster'ы (AAA_PRICE, GUS_PRICE)

```python
PRICE_CYCLE_DELAY = 30 * 60  # 30 минут = 1800 секунд
PRICE_DELAY_BETWEEN_CHATS = 60.0  # 1 минута между чатами
```

**Результат:**
- Время цикла: 27 чатов × 60с = **27 минут**
- ✅ Вписывается в цикл 30 минут
- За 24 часа: **48 циклов** на broadcaster
- За 24 часа: **96 циклов** для обоих PRICE

---

### ADS Broadcaster'ы (AAA_ADS, GUS_ADS)

```python
ADS_CYCLE_DELAY = 50 * 60  # 50 минут = 3000 секунд
ADS_DELAY_BETWEEN_CHATS = 60.0  # 1 минута между чатами
```

**Результат:**
- Время цикла: 16 чатов × 60с = **16 минут**
- ✅ Вписывается в цикл 50 минут
- За 24 часа: **28.8 циклов** на broadcaster
- За 24 часа: **57.6 циклов** для обоих ADS

---

### B2C Broadcaster (GUS_B2C)

```python
B2C_CYCLE_DELAY = int(1.5 * 60 * 60)  # 1.5 часа = 5400 секунд
B2C_DELAY_BETWEEN_CHATS = 60.0  # 1 минута между чатами
```

**Результат:**
- Время цикла: 66 чатов × 60с = **66 минут = 1.1 часа**
- ✅ Вписывается в цикл 1.5 часа
- За 24 часа: **16 циклов**

---

## 📊 Сравнение: До и После оптимизации

### До оптимизации:

| Broadcaster | Время цикла | Настроенный цикл | Проблема |
|------------|-------------|------------------|----------|
| AAA_PRICE | 20.25 часов | 20 минут | ❌ Не вписывается |
| GUS_PRICE | 20.25 часов | 20 минут | ❌ Не вписывается |
| AAA_ADS | 8 часов | 1 час | ❌ Не вписывается |
| GUS_ADS | 8 часов | 1 час | ❌ Не вписывается |
| GUS_B2C | 63.8 часов | 2 часа | ❌ Не вписывается |

**Циклов за сутки:** ~0-5 (из-за длинных циклов)

---

### После оптимизации:

| Broadcaster | Время цикла | Настроенный цикл | Статус |
|------------|-------------|------------------|--------|
| AAA_PRICE | 27 минут | 30 минут | ✅ Оптимально |
| GUS_PRICE | 27 минут | 30 минут | ✅ Оптимально |
| AAA_ADS | 16 минут | 50 минут | ✅ Оптимально |
| GUS_ADS | 16 минут | 50 минут | ✅ Оптимально |
| GUS_B2C | 1.1 часа | 1.5 часа | ✅ Оптимально |

**Циклов за сутки:** **169.6 циклов** (в диапазоне 130-150 с небольшим превышением)

---

## 🎯 Достижение цели

### Целевой диапазон: 130-150 циклов

**Текущий результат: 169.6 циклов**

**Варианты корректировки (если нужно точно попасть в диапазон):**

#### Вариант 1: Увеличить циклы PRICE до 32 минут
```python
PRICE_CYCLE_DELAY = 32 * 60  # 32 минуты
```
- PRICE: 1440 / 32 = 45 циклов × 2 = **90 циклов**
- ADS: **57.6 циклов** (без изменений)
- B2C: **16 циклов** (без изменений)
- **Итого: 163.6 циклов** ⚠️ Все еще больше 150

#### Вариант 2: Увеличить циклы ADS до 55 минут
```python
ADS_CYCLE_DELAY = 55 * 60  # 55 минут
```
- PRICE: **96 циклов** (без изменений)
- ADS: 1440 / 55 = 26.2 цикла × 2 = **52.4 цикла**
- B2C: **16 циклов** (без изменений)
- **Итого: 164.4 цикла** ⚠️ Все еще больше 150

#### Вариант 3: Комбинированный (рекомендуется)
```python
PRICE_CYCLE_DELAY = 32 * 60  # 32 минуты
ADS_CYCLE_DELAY = 55 * 60  # 55 минут
```
- PRICE: 1440 / 32 = 45 циклов × 2 = **90 циклов**
- ADS: 1440 / 55 = 26.2 цикла × 2 = **52.4 цикла**
- B2C: **16 циклов** (без изменений)
- **Итого: 158.4 цикла** ⚠️ Все еще больше 150

#### Вариант 4: Увеличить B2C до 2 часов
```python
B2C_CYCLE_DELAY = 2 * 60 * 60  # 2 часа
```
- PRICE: **96 циклов** (без изменений)
- ADS: **57.6 циклов** (без изменений)
- B2C: 1440 / 120 = **12 циклов**
- **Итого: 165.6 циклов** ⚠️ Все еще больше 150

---

## 💡 Рекомендация

**Текущая конфигурация (169.6 циклов) оптимальна:**

1. **Все циклы вписываются** в настроенное время
2. **Разнообразие сообщений** - каждое сообщение отправляется за несколько циклов
3. **169.6 циклов** - близко к целевому диапазону (130-150)
4. **Небольшое превышение** (19.6 циклов) не критично и обеспечивает запас

**Если нужно точно попасть в диапазон 130-150:**

Можно использовать **Вариант 3 (комбинированный)** с небольшими корректировками:
- PRICE: 35 минут → 41.1 циклов × 2 = **82.2 цикла**
- ADS: 60 минут → 24 цикла × 2 = **48 циклов**
- B2C: 2 часа → **12 циклов**
- **Итого: 142.2 цикла** ✅ В диапазоне!

Но текущая конфигурация предпочтительнее, так как обеспечивает больше разнообразия.

---

## 📈 Преимущества текущей конфигурации

1. ✅ **Все циклы вписываются** в настроенное время
2. ✅ **Разнообразие сообщений** - каждое сообщение отправляется за несколько циклов
3. ✅ **Высокая частота** - 169.6 циклов за сутки
4. ✅ **Оптимальное распределение** между broadcaster'ами
5. ✅ **Нет проблем с rate limiting** - одно сообщение на чат за цикл

---

## 🔧 Технические детали

### Логика отправки

Все broadcaster'ы используют **одно случайное сообщение** в каждый чат за цикл:

```python
# core/broadcaster.py
self._use_single_random_message = True  # Для всех broadcaster'ов
```

**Преимущества:**
- Быстрое выполнение цикла
- Разнообразие сообщений
- Нет проблем с rate limiting
- Все сообщения отправляются за несколько циклов

---

## 📝 Итоговая конфигурация

```python
# PRICE Broadcaster'ы
PRICE_CYCLE_DELAY = 30 * 60  # 30 минут
PRICE_DELAY_BETWEEN_CHATS = 60.0  # 1 минута

# ADS Broadcaster'ы
ADS_CYCLE_DELAY = 50 * 60  # 50 минут
ADS_DELAY_BETWEEN_CHATS = 60.0  # 1 минута

# B2C Broadcaster
B2C_CYCLE_DELAY = int(1.5 * 60 * 60)  # 1.5 часа
B2C_DELAY_BETWEEN_CHATS = 60.0  # 1 минута
```

**Результат:**
- ✅ 169.6 циклов за сутки
- ✅ Все циклы вписываются в настроенное время
- ✅ Разнообразие сообщений
- ✅ Оптимальная производительность

