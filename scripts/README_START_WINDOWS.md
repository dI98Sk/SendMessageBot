# 🚀 Скрипты первоначального запуска на Windows

## 📋 Описание

Скрипты для автоматического первоначального запуска SendMessageBot на Windows сервере.

Выполняют все необходимые шаги:
1. ✅ Создание виртуального окружения
2. ✅ Активация виртуального окружения
3. ✅ Установка зависимостей
4. ✅ Настройка Telegram аккаунтов
5. ✅ Запуск бота

---

## 📁 Файлы

- **`start_windows.bat`** - Batch скрипт (рекомендуется)
- **`start_windows.ps1`** - PowerShell скрипт (альтернатива)

---

## 🚀 Использование

### Вариант 1: Batch скрипт (рекомендуется)

```cmd
cd C:\path\to\SendMessageBot
scripts\start_windows.bat
```

Или просто дважды кликните на файл `start_windows.bat`.

### Вариант 2: PowerShell скрипт

```powershell
cd C:\path\to\SendMessageBot
.\scripts\start_windows.ps1
```

**Примечание:** Если получаете ошибку выполнения скриптов, выполните:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

---

## 📝 Что делает скрипт

### Шаг 1: Создание виртуального окружения
- Проверяет наличие `venv`
- Создает виртуальное окружение, если его нет
- Пропускает, если уже существует

### Шаг 2: Активация виртуального окружения
- Активирует `venv\Scripts\activate`
- Проверяет успешность активации

### Шаг 3: Установка зависимостей
- Устанавливает пакеты из `requirements.txt`
- Обновляет pip при необходимости

### Шаг 4: Настройка Telegram аккаунтов
- Проверяет наличие файлов сессий
- Запускает `scripts\setup_accounts.py`, если сессий нет
- Пропускает, если сессии уже существуют

### Шаг 5: Проверка конфигурации
- Проверяет наличие файла `.env`
- Предупреждает, если файл отсутствует
- Позволяет продолжить без `.env` (не рекомендуется)

### Шаг 6: Запуск бота
- Запускает `python main.py`
- Обрабатывает ошибки и выводит сообщения

---

## ⚙️ Требования

1. **Python 3.8+** установлен и добавлен в PATH
2. **Файл requirements.txt** присутствует
3. **Файл .env** настроен (или переменные окружения)
4. **Права на выполнение** скриптов (для PowerShell)

---

## 🔧 Настройка перед запуском

### 1. Создайте файл `.env`

Скопируйте `.env.example` в `.env` и заполните:

```env
# Telegram API
API_ID=your_api_id
API_HASH=your_api_hash

# Google Sheets
BUY_SELL_PRICE_AAA_SHEET_URL=https://...
BUY_SELL_PRICE_GUS_SHEET_URL=https://...
ADS_AAA_SHEET_URL=https://...
ADS_GUS_SHEET_URL=https://...
GOOGLE_CREDENTIALS_FILE=credentials.json

# И другие настройки...
```

### 2. Подготовьте credentials.json

Скачайте файл с учетными данными Google Sheets API и поместите в корень проекта.

---

## 🆘 Решение проблем

### Ошибка: "Python не найден"
- Установите Python 3.8+ с официального сайта
- При установке отметьте "Add Python to PATH"
- Перезапустите командную строку

### Ошибка: "Не удается загрузить файл"
- Для PowerShell: выполните `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser`
- Или используйте Batch скрипт вместо PowerShell

### Ошибка: "requirements.txt не найден"
- Убедитесь, что запускаете скрипт из корневой директории проекта
- Проверьте наличие файла `requirements.txt`

### Ошибка: "Файл .env не найден"
- Создайте файл `.env` на основе `.env.example`
- Или настройте переменные окружения вручную

### Ошибка при установке зависимостей
- Проверьте подключение к интернету
- Убедитесь, что pip обновлен: `python -m pip install --upgrade pip`
- Попробуйте установить зависимости вручную

---

## 📊 Логи

После запуска бота логи сохраняются в:
- `logs/bot.log` - основной лог
- Консольный вывод - для мониторинга в реальном времени

---

## 🔄 Повторный запуск

После первоначальной настройки можно запускать бота напрямую:

```cmd
venv\Scripts\activate
python main.py
```

Или используйте скрипт запуска службы (если настроена).

---

## 💡 Советы

1. **Первый запуск:** Используйте скрипт для первоначальной настройки
2. **Последующие запуски:** Можно запускать `main.py` напрямую
3. **Автозапуск:** Настройте Windows Task Scheduler для автоматического запуска
4. **Мониторинг:** Следите за логами в `logs/bot.log`

---

## 📚 Дополнительная документация

- [docs/setup/START_HERE.md](../docs/setup/START_HERE.md) - Полное руководство по настройке
- [docs/DEPLOYMENT_INSTRUCTIONS.md](../docs/DEPLOYMENT_INSTRUCTIONS.md) - Инструкции по деплою
- [docs/WINDOWS_SETUP.md](../docs/WINDOWS_SETUP.md) - Настройка на Windows

