# 🔍 Диагностика проблемы с обновлением сообщений на сервере

## 🎯 Быстрая диагностика (на сервере)

### Шаг 1: Создать папку sessions
```powershell
cd C:\Users\Administrator\PycharmProjects\SendMessageBot_v2
mkdir sessions
```

### Шаг 2: Проверить обновление сообщений
```powershell
python test_auto_update.py
```

Этот скрипт покажет:
- ✅ Какие Google Sheets настроены
- ✅ Есть ли credentials.json
- ✅ Загружаются ли сообщения
- ✅ Сколько сообщений в каждом типе

---

## 📋 Возможные проблемы и решения

### Проблема 1: Нет credentials.json

**Симптом:**
```
❌ Файл НЕ НАЙДЕН: credentials.json
```

**Решение:**
Скопируйте `credentials.json` с локальной машины на сервер в корень проекта.

---

### Проблема 2: URL Google Sheets не настроены

**Симптом:**
```
❌ AAA Price: НЕ НАСТРОЕН
❌ GUS Price: НЕ НАСТРОЕН
```

**Решение:**
Проверьте `.env` файл на сервере, должны быть:
```
BUY_SELL_PRICE_AAA_SHEET_URL=https://docs.google.com/...
BUY_SELL_PRICE_GUS_SHEET_URL=https://docs.google.com/...
ADS_AAA_SHEET_URL=https://docs.google.com/...
ADS_GUS_SHEET_URL=https://docs.google.com/...
```

---

### Проблема 3: Нет доступа к Google Sheets

**Симптом:**
```
❌ aaa_price: ОШИБКА
   Ошибка: Permission denied
```

**Решение:**
1. Проверьте что credentials.json валиден
2. Проверьте что сервисный аккаунт имеет доступ к таблицам
3. В Google Sheets → Share → добавьте email из credentials.json

---

### Проблема 4: Файл utils/auto_updater.py отсутствует

**Симптом:**
```
ModuleNotFoundError: No module named 'utils.auto_updater'
```

**Решение:**
Скопируйте с локальной машины:
```
utils/auto_updater.py
```

---

## 🧪 Альтернативный тест (без auto_updater)

Если `test_auto_update.py` не работает, попробуйте прямую загрузку:

```powershell
python scripts/update_messages.py
python scripts/update_ads_messages.py
```

Эти скрипты обновят сообщения напрямую.

---

## ✅ Проверка после обновления

```powershell
# Проверить что файлы обновились
dir config\messages_*.py

# Посмотреть количество сообщений
python -c "from config.messages_aaa import MESSAGESAAA; print(f'AAA: {len(MESSAGESAAA)} сообщений')"
python -c "from config.messages_gus import MESSAGESGUS; print(f'GUS: {len(MESSAGESGUS)} сообщений')"
```

---

## 🚀 После успешного обновления

```powershell
python main.py
```

Должно показать актуальное количество сообщений:
```
✅ AAA PRICE Broadcaster: 22 чатов, N сообщений
✅ GUS PRICE Broadcaster: 22 чатов, N сообщений
```

Где N - количество из Google Sheets.

---

## 📝 Чек-лист для сервера

- [ ] Папка `sessions` создана
- [ ] Файл `credentials.json` существует
- [ ] В `.env` настроены все 4 URL Google Sheets
- [ ] Файл `utils/auto_updater.py` существует
- [ ] `test_auto_update.py` успешно выполняется
- [ ] Файлы `messages_*.py` обновлены
- [ ] `python main.py` запускается без ошибок

---

**Запустите `test_auto_update.py` на сервере и пришлите результат!**

