# 🔄 Синхронизация кода с сервером

## ❌ Проблема

На сервере запускается старая версия:
- Только 1 broadcaster вместо 4
- Используются тестовые чаты вместо production
- Ошибка `ValueError: invalid literal for int()`

**Причина:** На сервере старый код!

---

## ✅ Решение: Синхронизация файлов

### Вариант 1: Копирование всего проекта (РЕКОМЕНДУЕТСЯ)

**На локальной машине (Mac):**
```bash
cd /Users/papaskakun/PycharmProjects

# Создать архив (без .env и .venv)
tar -czf SendMessageBot.tar.gz \
  --exclude='.env' \
  --exclude='.venv' \
  --exclude='__pycache__' \
  --exclude='*.pyc' \
  --exclude='*.log' \
  --exclude='*.session-journal' \
  SendMessageBot/
```

**Скопировать на сервер и распаковать:**
```powershell
# На сервере (Windows PowerShell)
cd C:\Users\Administrator\PycharmProjects

# Сохранить .env файл
copy SendMessageBot\.env SendMessageBot_env_backup.txt

# Распаковать (используйте WinRAR, 7-Zip или tar для Windows)
tar -xzf SendMessageBot.tar.gz

# Вернуть .env файл
copy SendMessageBot_env_backup.txt SendMessageBot\.env
```

---

### Вариант 2: Копирование только измененных файлов

**Скопируйте на сервер следующие файлы:**

#### Критически важные:
```
main.py                          ⭐ ГЛАВНЫЙ!
config/settings.py               ⭐ (int→float)
core/broadcaster.py              (cycle_delay)
utils/auto_updater.py            (новый файл!)
monitoring/reports.py            (timezone fix)
```

#### Конфигурация:
```
config/targets.py                (TEST_TARGETS_ADS)
config/messages_aaa_ads.py       (новый!)
config/messages_gus_ads.py       (новый!)
```

#### Дополнительные:
```
main_test.py                     (тестовая версия)
scripts/update_ads_messages.py   (обновление рекламы)
```

#### Сессии:
```
sessions/acc1_price.session
sessions/acc2_price.session
sessions/acc1_ads.session
sessions/acc2_ads.session
```

---

### Вариант 3: Создание сессий на сервере

Если не хотите копировать файлы сессий, создайте их на сервере:

```powershell
cd C:\Users\Administrator\PycharmProjects\SendMessageBot\sessions

# Копировать существующие сессии
copy acc1.session acc1_price.session
copy acc1.session acc1_ads.session
copy acc2.session acc2_price.session
copy acc2.session acc2_ads.session
```

---

## 🧪 Проверка после синхронизации

### На сервере запустите:

```bash
python test_new_broadcasters.py
```

**Ожидаемый результат:**
```
✅ AAA PRICE Broadcaster создан (acc1): 22 чатов, 13 сообщений, цикл: 20 мин
✅ GUS PRICE Broadcaster создан (acc2): 22 чатов, 4 сообщений, цикл: 20 мин
✅ AAA ADS Broadcaster создан (acc2): 11 чатов, 1 сообщений, цикл: 1 час
✅ GUS ADS Broadcaster создан (acc1): 11 чатов, 1 сообщений, цикл: 1 час
📊 Всего broadcaster'ов: 4

✅ ВСЕ BROADCASTER'Ы СОЗДАНЫ УСПЕШНО!
```

### Если видите только 1 broadcaster - код еще не обновлен!

---

## 📋 Чек-лист синхронизации

Убедитесь что на сервере:

### Файлы обновлены:
- [ ] `main.py` - должен создавать 4 broadcaster'а
- [ ] `config/settings.py` - должен использовать `float` для отчетов
- [ ] `core/broadcaster.py` - должен иметь параметр `cycle_delay`
- [ ] `utils/auto_updater.py` - должен существовать

### Конфигурация:
- [ ] `.env` файл сохранен (с вашими настройками)
- [ ] `credentials.json` на месте

### Сессии:
- [ ] 6 файлов сессий существуют (2 оригинала + 4 уникальных)

### Проверка:
- [ ] `python test_new_broadcasters.py` → 4 broadcaster'а
- [ ] Нет ошибок при импорте

---

## 🚀 После синхронизации

```bash
python main.py
```

Должно быть:
```
✅ AAA PRICE Broadcaster создан (acc1): 22 чатов, N сообщений, цикл: 20 мин
✅ GUS PRICE Broadcaster создан (acc2): 22 чатов, N сообщений, цикл: 20 мин
✅ AAA ADS Broadcaster создан (acc2): 11 чатов, N сообщений, цикл: 1 час
✅ GUS ADS Broadcaster создан (acc1): 11 чатов, N сообщений, цикл: 1 час
📊 Всего broadcaster'ов: 4
🔄 Автообновление сообщений включено
```

---

## 💡 Самый простой способ

**Скопируйте всю папку проекта целиком с Mac на Windows сервер!**

Все файлы уже исправлены и готовы к работе.

---

*Дата: 2025-11-04*

