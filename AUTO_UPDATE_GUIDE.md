# 🔄 Автоматическое обновление сообщений

## ✅ Что настроено

Автоматическое обновление **ВСЕХ** типов сообщений из Google Sheets:

1. **Прайсы AAA** (из `BUY_SELL_PRICE_AAA_SHEET_URL`)
2. **Прайсы GUS** (из `BUY_SELL_PRICE_GUS_SHEET_URL`)
3. **Реклама AAA** (из `ADS_AAA_SHEET_URL`)
4. **Реклама GUS** (из `ADS_GUS_SHEET_URL`)

## 🎯 Как это работает

### При запуске бота:
1. ✅ Сразу выполняется первоначальное обновление всех сообщений
2. ✅ Загружаются данные из всех 4 Google Sheets
3. ✅ Обновляются файлы конфигурации:
   - `config/messages_aaa.py`
   - `config/messages_gus.py`
   - `config/messages_aaa_ads.py`
   - `config/messages_gus_ads.py`
4. ✅ Broadcaster'ы создаются с актуальными сообщениями

### Во время работы:
1. ⏰ Каждые N часов (по настройке) запускается автообновление
2. 📥 Загружаются новые сообщения из Google Sheets
3. 💾 Обновляются файлы конфигурации
4. 🔄 Broadcaster'ы **автоматически пересоздаются** с новыми сообщениями
5. 📊 **Система отчетов продолжает работать** (не прерывается!)
6. 📢 Отправляется уведомление об обновлении

## ⚙️ Настройка интервала обновления

В `.env`:
```bash
# Интервал обновления сообщений (в секундах)
GOOGLE_UPDATE_INTERVAL=3600  # 1 час (рекомендуется)
```

### Примеры интервалов:

```bash
# Каждые 30 минут
GOOGLE_UPDATE_INTERVAL=1800

# Каждый час (по умолчанию)
GOOGLE_UPDATE_INTERVAL=3600

# Каждые 2 часа
GOOGLE_UPDATE_INTERVAL=7200

# Каждые 6 часов
GOOGLE_UPDATE_INTERVAL=21600
```

## 📊 Система отчетов

### ✅ Защита от прерывания

При автоматическом обновлении и пересоздании broadcaster'ов:

- ✅ **Задача отчетов сохраняется** и не останавливается
- ✅ Отчеты продолжают отправляться по расписанию
- ✅ Не теряется статистика
- ✅ Не сбрасывается счетчик отправленных отчетов

### Логирование

В логах вы увидите:
```
[INFO] auto_updater: 🔄 Начинаем обновление всех сообщений из Google Sheets...
[INFO] auto_updater: 📥 Загрузка прайсов AAA...
[INFO] auto_updater: ✅ Прайсы AAA обновлены: 13 сообщений
[INFO] auto_updater: 📥 Загрузка прайсов GUS...
[INFO] auto_updater: ✅ Прайсы GUS обновлены: 4 сообщения
[INFO] auto_updater: 📥 Загрузка рекламы AAA...
[INFO] auto_updater: ✅ Реклама AAA обновлена: 5 сообщений
[INFO] auto_updater: 📥 Загрузка рекламы GUS...
[INFO] auto_updater: ✅ Реклама GUS обновлена: 3 сообщения
[INFO] main: 🔄 Пересоздание broadcaster'ов с новыми сообщениями...
[INFO] main: 💾 Сохранена задача системы отчетов
[INFO] main: ✅ Все broadcaster'ы запущены после пересоздания
[INFO] main: ✅ Система отчетов продолжает работать
```

## 🔍 Мониторинг автообновления

### Проверка статуса

В логах:
```bash
tail -f bot.log | grep auto_updater
```

### Счетчики

Автообновлятель ведет учет:
- `updates_count` - успешных обновлений
- `errors_count` - ошибок обновления
- `last_update_time` - время последнего обновления

## 📝 Уведомления

При каждом автообновлении отправляется уведомление в Telegram:

```
🔄 Сообщения автоматически обновлены

Обновлено: aaa_price=13, gus_price=4, aaa_ads=5, gus_ads=3
Broadcaster'ы перезапущены с новыми сообщениями
```

## ⚠️ Обработка ошибок

### Если не удалось обновить один тип:
- ✅ Другие типы обновятся
- ⚠️ Будет предупреждение в логах
- ✅ Broadcaster'ы пересоздадутся с теми, что обновились

### Если не удалось ни одного:
- ⚠️ Пересоздание не выполняется
- ⚠️ Отправляется уведомление об ошибке
- ✅ Broadcaster'ы продолжают работать со старыми сообщениями

### Если ошибка при пересоздании:
- ❌ Broadcaster'ы останутся остановленными
- 📢 Отправится критическое уведомление
- 💡 Потребуется перезапуск бота

## 🧪 Тестирование

### Проверка автообновления

```bash
# Запустить бот
python main.py

# В другом терминале следить за логами
tail -f bot.log | grep -E "(auto_updater|Пересоздание)"
```

При запуске должно быть:
```
✅ Автоматический обновлятель сообщений настроен
🔄 Автообновление сообщений включено (каждые 1.0 часов)
   • Прайсы AAA/GUS
   • Реклама AAA/GUS
```

### Ручное обновление (по-прежнему работает)

```bash
# Обновить прайсы
python scripts/update_messages.py

# Обновить рекламу
python scripts/update_ads_messages.py
```

Но теперь это не обязательно - бот сам обновит!

## 🎛️ Управление

### Отключить автообновление

В `.env`:
```bash
# Установить очень большой интервал (практически отключит)
GOOGLE_UPDATE_INTERVAL=86400  # 24 часа
```

Или просто не указывать URL Google Sheets.

### Изменить интервал на лету

Нужно перезапустить бот после изменения `.env`.

## 📊 Сравнение: было → стало

### Было (без автообновления):

1. Редактировали Google Sheets
2. Вручную запускали скрипты:
   - `python scripts/update_messages.py`
   - `python scripts/update_ads_messages.py`
3. Перезапускали бот для применения изменений

### Стало (с автообновлением):

1. Редактируете Google Sheets
2. **Всё! Больше ничего не нужно!**
3. Через N часов бот сам:
   - Загрузит новые сообщения
   - Обновит конфигурацию
   - Перезапустит broadcaster'ы
   - Продолжит отправку отчетов
   - Уведомит вас об обновлении

## ✅ Преимущества

1. **Автоматизация** - не нужно запускать скрипты вручную
2. **Актуальность** - сообщения всегда свежие
3. **Надежность** - система отчетов не прерывается
4. **Уведомления** - вы всегда в курсе обновлений
5. **Безопасность** - обработка ошибок на каждом этапе
6. **Гибкость** - настраиваемый интервал обновления

## 🔧 Техническая информация

### Компоненты:

1. **AutoMessageUpdater** (`utils/auto_updater.py`)
   - Загрузка сообщений из Google Sheets
   - Сохранение в файлы конфигурации
   - Периодический цикл обновления

2. **Интеграция в main.py**
   - Инициализация при запуске
   - Callback для обработки обновлений
   - Защита системы отчетов при пересоздании

3. **Умное пересоздание broadcaster'ов**
   - Остановка старых broadcaster'ов
   - Сохранение задачи отчетов
   - Создание новых broadcaster'ов
   - Запуск с новыми сообщениями

### Файлы:

- `utils/auto_updater.py` - автообновлятель
- `main.py` - интеграция в основное приложение
- `config/messages_*.py` - автоматически обновляемые файлы

---

**🚀 Автообновление настроено и готово к работе!**

*Дата: 2025-11-04*
*Версия: 1.0*


